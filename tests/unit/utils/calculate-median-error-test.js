import calculateMedianError from 'labs-nyc-factfinder/utils/calculate-median-error';
import { module, test } from 'qunit';

module('Unit | Utility | calculate median error');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = calculateMedianError();
  assert.ok(result);
});
