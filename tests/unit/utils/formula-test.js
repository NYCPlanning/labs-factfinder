import formula from 'labs-nyc-factfinder/utils/formula';
import { module, test } from 'qunit';

module('Unit | Utility | formula');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formula();
  assert.ok(result);
});
