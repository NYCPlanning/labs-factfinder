import guessYear from 'labs-nyc-factfinder/utils/guess-year';
import { module, test } from 'qunit';

module('Unit | Utility | guess year');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = guessYear();
  assert.ok(result);
});
