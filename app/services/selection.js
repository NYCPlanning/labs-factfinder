import Ember from 'ember';
import { computed } from 'ember-decorators/object';

export default Ember.Service.extend({
  current: null,

  addToSelection(current) {
    if (current) {
      // turfjs combination of features
    }
  },
});
