{{#profile-header
  profile=profile
  title="Demographic Profile"
  mode=profile.mode
  data=rawData
}}
  <p class="text-tiny">
    ACS data are derived from a survey and are subject to sampling variability.
    Grayed values are not statistically reliable (<a href="https://www.census.gov/programs-surveys/acs/guidance.html" target="_blank">guidance on ACS data</a>).

    Data associated with estimates of zero, top- and bottom-coded estimates, or Coefficients of Variation (CVs) of 20% or more are grayed out to signify poor statistical reliability. Comparisons and changes over time that are not statistically significant at a 90% confidence level are also grayed out.
  </p>
{{/profile-header}}

<hr>

<h3 class="header-medium">Age and Sex</h3>
<div class="grid-x grid-margin-x">
  <div class="cell large-8">
    {{acs-table
      scrollTable=profile.scrollTable
      mode=profile.mode
      reliability=profile.reliability
      comparison=profile.comparison
      config=sexAge
      t1=model.y2011_2015.sex_and_age
      t2=model.y2006_2010.sex_and_age
    }}
  </div>

  <div class="cell large-4">
    <div class="callout">
      {{population-pyramid data=agePopDist}}
    </div>
  </div>
</div>

<hr>

<h3 class="header-medium">Mutually Exclusive Race / Hispanic Origin</h3>
<div class="grid-x grid-margin-x">
  <div class="cell large-8">

    {{acs-table
      scrollTable=profile.scrollTable
      mode=profile.mode
      reliability=profile.reliability
      comparison=profile.comparison
      config=raceGroup
      t1=model.y2011_2015.mutually_exclusive_race_hispanic_origin
      t2=model.y2006_2010.mutually_exclusive_race_hispanic_origin
    }}
  </div>
{{log model}}
  <div class="cell large-4">
    <div class="callout">
      {{acs-bar data=racialProfile height=150 barLabel=false xMax=1}}
    </div>
  </div>
</div>

<hr>

<h3 class="header-medium">Hispanic Subgroup</h3>
<div class="grid-x grid-margin-x">
  <div class="cell large-8">
    {{acs-table
      scrollTable=profile.scrollTable
      mode=profile.mode
      reliability=profile.reliability
      comparison=profile.comparison
      config=hispanicSubgroup
      t1=model.y2011_2015.hispanic_subgroup
      t2=model.y2006_2010.hispanic_subgroup
    }}
  </div>
</div>

<hr>

<h3 class="header-medium">Asian Subgroup</h3>
<div class="grid-x grid-margin-x">
  <div class="cell large-8">

      {{acs-table
        scrollTable=profile.scrollTable
        mode=profile.mode
        reliability=profile.reliability
        comparison=profile.comparison
        config=asianSubgroup
        t1=model.y2011_2015.asian_subgroup
        t2=model.y2006_2010.asian_subgroup
      }}
  </div>
</div>
{{outlet}}
