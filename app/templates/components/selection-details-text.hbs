<strong>
  {{#if (eq @selectedType 'cities')}}
    New York City
  {{else}}
    {{@selectedCount}}
    {{if (eq @selectedType 'tracts') 'Tract'~}}
    {{if (eq @selectedType 'blocks') 'Block'~}}
    {{if (eq @selectedType 'cdtas') 'CDTA'~}}
    {{if (eq @selectedType 'districts') 'CD'~}}
    {{if (eq @selectedType 'ntas') 'NTA'~}}
    {{if (eq @selectedType 'boroughs') 'Borough'~}}
    {{unless (eq @selectedCount 1) 's'}}
  {{/if}}
</strong>
{{#unless (eq @selectedType 'cities')}}
   | 
{{/unless}}
